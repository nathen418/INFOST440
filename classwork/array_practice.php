<?php
// Create any array with at least 5 elements. Seed with random first names
$names = array('John', 'Jane', 'Joe', 'Jill', 'Jack');

// Print at least one element from the array
echo $names[0];

// Use foreach loop to print every element in the array.
foreach ($names as $name) {
    echo $name;
}
?>

<!-- Worked with Nathan -->